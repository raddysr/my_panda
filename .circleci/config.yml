version: 2
jobs:
  build1:
    docker:
      - image: circleci/python:3.8
    steps:
      - checkout
      - run: echo "A first test"
  build2:
    docker:
      - image: circleci/python:3.8
    steps:
      - checkout
      - run: echo "A second test"
workflows:
  version: 2
  test_builds:
    jobs:
      - build1
      - build2